export enum UserRole {
  Admin = "admin",
  Contributor = "contributor",
  Viewer = "viewer",
}

export enum QuestionStatus {
  Pending = "pending",
  Approved = "approved",
  Rejected = "rejected",
}
